# FinGuard AI

FinGuard AI — Personal Finance & Contract Risk Analysis Platform. FastAPI + MongoDB backend, HTML/CSS/JS Claymorphism frontend. See docs/API_CONTRACT.md for the shared endpoint contract between team members. Setup: python3 -m venv venv, source venv/bin/activate, pip install -r requirements.txt, then uvicorn app.main:app --reload
